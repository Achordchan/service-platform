.page {
  padding: 20rpx 0 60rpx;
}

.placeholder {
  color: var(--text-secondary);
}

/* 多行：撤掉 .input 的单行固定高度与行高 */
.textarea {
  height: auto;
  min-height: 280rpx;
  line-height: 1.6;
  padding: 20rpx 24rpx;
  width: auto;
}

.disclosure {
  margin-top: 24rpx;
  padding: 16rpx 20rpx;
  border-radius: 12rpx;
  background: var(--brand-soft);
  color: var(--text-secondary);
  font-size: 22rpx;
  line-height: 1.6;
}

.submit-btn {
  margin-top: 36rpx;
}

.submit-hint {
  text-align: center;
  margin-top: 14rpx;
  font-size: 22rpx;
  color: var(--text-secondary);
}
