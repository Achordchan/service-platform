# http {} context only
# Never log query strings for Sub2API iframe entrypoints that may carry JWT.
log_format main_no_args '$remote_addr - $remote_user [$time_local] '
                        '"$request_method $uri $server_protocol" $status $body_bytes_sent '
                        '"$http_referer" "$http_user_agent"';
