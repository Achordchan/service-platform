# DEPRECATED as a single include: Nginx forbids mixing log_format (http) and
# location (server) in one snippet.
#
# Use:
#   scripts/nginx-sub2api-embed-log-format.conf   -> include inside http {}
#   scripts/nginx-sub2api-embed-location.conf     -> include inside the site server {}
#
# remote-deploy.sh installs both files and verifies they are referenced by live
# configs (not merely present under /etc/nginx/snippets).
